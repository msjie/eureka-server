server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    # standalone mode not register eureka and not fetch registry
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

